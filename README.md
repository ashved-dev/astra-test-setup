# Astra Testing Setup

Minimal smoke project for validating the Astra remote delivery path:

Codex Desktop -> Astra MCP/API -> remote worker -> GitHub -> Coolify.

The implementation target is a small Vite, React, and TypeScript todo app with
localStorage persistence, unit/integration tests, and a Playwright smoke test.
